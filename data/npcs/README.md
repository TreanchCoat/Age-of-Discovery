# data/npcs/

Drop `NPCDef` `.tres` files here — NPCDB auto-loads the folder at startup.
NPCs with a `home_city` spawn in that city's street mode automatically.
Copy-paste template: `DOCUMENTATION.md` §11.
